"use strict";

function submit() {
    const name = document.querySelector(`.name`).value;
    const rollNumber = document.querySelector(`.roll`).value;
}
document.querySelector(".submit").addEventListener("click", submit);